<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mongo="http://www.springframework.org/schema/data/mongo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
		http://www.springframework.org/schema/data/mongo http://www.springframework.org/schema/data/mongo/spring-mongo-1.2.xsd">
		
		<context:property-placeholder location="classpath:/META-INF/mongo.properties"/>
		
		<mongo:mong host="${mongo.host}" port="mongo.port">
			<mongo:options 
				connnections-per-host="${mongo.connections.per.host}"
				auto-connection-retry="${mongo.auto.connect.retry}"
				connection-timeout="${mongo.connection.timeout}"
				max-wait-time="${mongo.max.wait.time}"/>
		</mongo:mong>
		
		<mongo:db-factory mongo-ref="mongo"/>
		
</beans>
